apiVersion: v1
kind: Service
metadata:
  name: homebanking-app
  labels:
    app: homebanking-app
    pr: "{{ .Values.prNumber }}"
spec:
  type: ClusterIP
  selector:
    app: homebanking-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
