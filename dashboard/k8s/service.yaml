apiVersion: v1
kind: Service
metadata:
  name: dashboard-app
  labels:
    app: dashboard-app
spec:
  type: ClusterIP
  selector:
    app: dashboard-app
  ports:
    - port: 80
      targetPort: 8085
      protocol: TCP
