auth:
  database: knowledge
  username: knowledge
  password: knowledge
primary:
  initdb:
    scripts:
      init.sql: |
        \c knowledge
        CREATE EXTENSION IF NOT EXISTS vector;
  persistence:
    enabled: true
    size: 5Gi
    storageClass: standard
  resources:
    requests:
      memory: 256Mi
      cpu: 100m
    limits:
      memory: 512Mi
