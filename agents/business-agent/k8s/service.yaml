apiVersion: v1
kind: Service
metadata:
  name: business-agent
  labels:
    app: business-agent
spec:
  type: ClusterIP
  selector:
    app: business-agent
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
