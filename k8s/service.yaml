apiVersion: v1
kind: Service
metadata:
  name: reacting-to-ai
  labels:
    app: reacting-to-ai
spec:
  type: ClusterIP
  selector:
    app: reacting-to-ai
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
